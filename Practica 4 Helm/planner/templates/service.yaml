apiVersion: v1
kind: Service
metadata:
  name: {{ include "planner.fullname" . }}
  labels:
    {{- include "planner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "planner.selectorLabels" . | nindent 4 }}
