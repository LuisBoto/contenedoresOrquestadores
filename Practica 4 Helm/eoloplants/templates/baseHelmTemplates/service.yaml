apiVersion: v1
kind: Service
metadata:
  name: {{ include "eoloplants.fullname" . }}
  labels:
    {{- include "eoloplants.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eoloplants.selectorLabels" . | nindent 4 }}
